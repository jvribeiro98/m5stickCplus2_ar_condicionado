{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openir.dev/schemas/v1/command.schema.json",
  "title": "OpenIR Legacy Command",
  "description": "DEPRECATED. Formato de migração para registros OpenIR 1.0 iniciais. Novos dados devem usar Binding + Signal.",
  "$comment": "Este schema é somente para leitura e migração. Será removido em uma futura versão major.",
  "deprecated": true,
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "canonical_name", "encoding_type", "legacy_payload", "notes"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^command-[a-z0-9]+(?:-[a-z0-9]+)*$"
    },
    "canonical_name": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(?:_[a-z0-9]+)*$"
    },
    "encoding_type": {
      "enum": ["protocol", "raw", "stateful"]
    },
    "legacy_payload": {
      "type": "object",
      "description": "Conteúdo original preservado para migração; não é um Signal canônico."
    },
    "notes": {
      "type": ["string", "null"],
      "maxLength": 4000
    }
  }
}
